//estrutura for para repetição


#include<stdio.h> //
int main(){
	int numero, contador;
	printf("Digite o número: ");
	scanf("%d", &numero);
//para = for
//de até
    autoincremento: contador++;
    for(contador = 10; contador <= 10; contador++){
       printf("%d x %d = %d \n", numero, contador, numero*contador);		
	}
		
}
